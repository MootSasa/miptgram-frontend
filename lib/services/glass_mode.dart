/// Режим Liquid Glass дизайна.
enum GlassMode {
  /// Стеклянный дизайн выключен — классический интерфейс
  disabled,

  /// Облегчённый режим — FakeGlass без тяжёлых шейдеров
  lite,

  /// Полный режим — полноценный LiquidGlass с шейдерами
  full,
}
